# .gitattributes
# Управление окончаниями строк и типами файлов

# Автоопределение текстовых файлов
* text=auto

# Python-файлы — сохраняем с LF (для кроссплатформенности)
*.py text eol=lf

# Конфигурационные и env-файлы — CRLF на Windows, но нормализуем в LF в репозитории
*.env text diff=env
*.env.example text diff=env
*.env.prod text diff=env
*.ini text eol=crlf

# Markdown — обычно CRLF на Windows
*.md text eol=crlf

# JSON, YAML — LF предпочтительно (особенно для CI/CD)
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Конфиги IDE (например, .idea от PyCharm)
*.xml text eol=crlf
.idea/**/* text

# HTML, CSS, JS — CRLF на Windows
*.html text eol=crlf
*.css text eol=crlf
*.js text eol=crlf
*.ts text eol=crlf
*.jsx text eol=crlf
*.tsx text eol=crlf

# Логи и временные файлы — не включать в Git (но если вдруг попали — как текст)
*.log text eol=crlf

# Бинарные файлы — явно помечаем
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
*.exe binary
*.dll binary
*.pyc binary
__pycache__/**
*.py[co]

# Избегаем проблем с путями в Windows
*.bat text eol=crlf
*.cmd text eol=crlf

# Git-файлы
.gitattributes text eol=lf
.gitignore text eol=lf